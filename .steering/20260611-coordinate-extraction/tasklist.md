# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクをより小さなサブタスクに分割する
- 分割したサブタスクをこのファイルに追加する
- サブタスクを1つずつ完了させる

---

## フェーズ1: 実装

- [x] Pythonプロジェクトの最小構成を作成する
  - [x] `.python-version` を作成する
  - [x] `pyproject.toml` を作成し、Typer、Pillow、pytest、ruff、basedpyrightを設定する
  - [x] `.gitignore` を作成し、`.venv/`、Pythonキャッシュ、ローカル実行データを除外する
  - [x] `src/coord_prompt_studio/` と `tests/` のパッケージ構成を作成する

- [x] コーデ抽出プロンプトテンプレートを作成する
  - [x] `prompts/coordinate_extraction.md` に実写・イラスト共通の抽出指示を記述する
  - [x] 不確実な要素を断定しない方針をテンプレートに含める
  - [x] 画像生成向けに貼り付けやすい出力形式をテンプレートに含める

- [x] 抽出セッションのドメインモデルを実装する
  - [x] 入力種別 `photo` / `illustration` を表す型を定義する
  - [x] 比較評価 `同等` / `不足` / `過剰` / `要改善` を表す型を定義する
  - [x] 入力画像、抽出結果、ChatGPT比較結果、比較評価、抽出セッションの型を定義する
  - [x] セッションの作成日時・更新日時を管理する
  - [x] 不正な入力種別・評価値を拒否する

- [x] JSONセッションリポジトリを実装する
  - [x] `data/sessions/` 配下へセッションJSONを保存する
  - [x] セッションIDから詳細を読み込む
  - [x] 保存済みセッションの一覧を作成日時順で取得する
  - [x] 存在しないセッションIDに分かりやすいエラーを返す

- [x] 画像検証アダプタを実装する
  - [x] 存在しない画像パスを拒否する
  - [x] `.jpg`、`.jpeg`、`.png`、`.webp` 以外の拡張子を拒否する
  - [x] Pillowで読み込み不能な画像を拒否する
  - [x] 検証済み画像パスをセッションへ保存できる形で返す

- [x] Codex CLIアダプタを実装する
  - [x] `prompts/coordinate_extraction.md` を読み込む
  - [x] 入力画像パス、入力種別、任意メモを含む指示文を組み立てる
  - [x] `codex` CLIをsubprocessで呼び出す
  - [x] Codex呼び出し失敗時の理由を抽出失敗として返す
  - [x] ユースケーステストでモックできる境界を用意する

- [x] コーデ抽出ユースケースを実装する
  - [x] 画像検証、Codex抽出、セッション保存を連携する
  - [x] 抽出成功時に抽出結果を保存する
  - [x] 抽出失敗時に失敗理由をセッションへ保存する
  - [x] 任意メモをセッションへ保存する

- [x] ChatGPT比較記録ユースケースを実装する
  - [x] 保存済みセッションへChatGPT抽出結果テキストを追加する
  - [x] 比較観点ごとの評価を保存する
  - [x] 自由記述メモを保存する
  - [x] 更新日時を更新する

- [x] セッション参照ユースケースを実装する
  - [x] 保存済みセッション一覧を取得する
  - [x] 保存済みセッション詳細を取得する
  - [x] 本ツール抽出結果とChatGPT抽出結果を同一セッションで確認できる形にする

- [x] CLIサブコマンドを実装する
  - [x] `extract` コマンドで画像パス、入力種別、任意メモを受け取る
  - [x] `add-chatgpt` コマンドでChatGPT抽出結果を保存する
  - [x] `evaluate` コマンドで比較評価と自由記述メモを保存する
  - [x] `list` コマンドでセッション一覧を表示する
  - [x] `show` コマンドでセッション詳細を表示する
  - [x] エラー時にCLI利用者へ分かりやすいメッセージを表示する

## フェーズ2: 品質チェック

- [x] テストが通ることを確認する
  - [x] `uv run pytest`
- [x] リントエラーがないことを確認する
  - [x] `uv run ruff check .`
  - [x] `uv run ruff format --check .`
- [x] 型チェックが通ることを確認する
  - [x] `uv run basedpyright`

## フェーズ3: ドキュメント更新

- [x] `README.md` を更新する（コマンド・操作手順に変更がある場合）
- [x] 関連する `docs/` ドキュメントを確認する（設計・アーキテクチャ変更なしのため更新不要）
- [x] 実装後の振り返りを記録する（このファイル下部）

---

## 実装後の振り返り

### 実装完了日
2026-06-12

### 計画と実績の差分

**計画と異なった点**:
- `uv` とPython 3.12が実行環境に未導入だったため、ワークスペース内の `.uv-cache` と `.uv-python` を使って検証環境を構築した
- `python -m coord_prompt_studio.cli` で起動できるよう、CLIモジュールに実行ガードを追加した

**新たに必要になったタスク**:
- 画像検証アダプタのテストを追加した
- basedpyrightが `.venv` を参照できるよう `pyproject.toml` に `venvPath` / `venv` を追加した

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- 該当なし

### 学んだこと

**技術的な学び**:
- Codex CLI呼び出しを値オブジェクトで成功・失敗として返すことで、抽出失敗理由をセッションに保存しやすくなった
- 日本語評価値は `StrEnum` で保存値を固定すると、CLI表示とJSON保存の揺れを抑えられる

**プロセス上の改善点**:
- devcontainer内でも `uv` のキャッシュ先・Pythonインストール先が書き込み可能かを先に確認すると検証が早い

### 次回への改善提案
- `codex` CLIへ画像を渡す実運用コマンドは、実画像を使った手動検証で調整する
- ChatGPT抽出結果の入力は、長文貼り付け向けにエディタ起動や標準入力対応を検討する

### リリース判断

**前提条件の確認**:
- [x] 全テスト通過
- [x] リントエラーなし

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | MINOR |

**提案**:
MVP 0の最小CLIとしてPR作成可能。実画像とCodex CLIの組み合わせは、PR後または次Issueで手動検証を行う。
