# 技術仕様書 (Architecture Document)

## システム概要

<!-- 例:
CLI ツールとして動作し、ローカルの SQLite データベースにタスクを保存する。
外部依存は GitHub API のみ。
-->
[システムの全体像を 2〜3 文で記述]

## アーキテクチャ図

```
[テキストまたは Mermaid 形式でアーキテクチャを図示]

例（レイヤー構造）:
┌─────────────────┐
│   CLI レイヤー   │  ← ユーザー入力を受け付ける
├─────────────────┤
│ ユースケース層   │  ← ビジネスロジック
├─────────────────┤
│  データ層        │  ← SQLite / ファイルシステム
└─────────────────┘
```

## 技術スタック

| カテゴリ | 採用技術 | 選定理由 |
|---------|---------|---------|
| 言語 | [例: Python 3.12] | [例: 型ヒントが充実しており、保守性が高い] |
| フレームワーク | [例: なし / FastAPI / etc.] | [選定理由] |
| データストア | [例: SQLite / PostgreSQL / etc.] | [選定理由] |
| テスト | [例: pytest] | [選定理由] |
| リンター | [例: ruff] | [選定理由] |
| 型チェック | [例: basedpyright] | [選定理由] |

## 主要コンポーネント

### [コンポーネント名1]

**役割**: [このコンポーネントが担う責務]

**主要ファイル**:
<!-- 例: src/services/task_service.py -->
- `[ファイルパス]` — [役割]

**依存関係**:
<!-- 例: UserRepository, NotificationService -->
- [依存するコンポーネント]

---

### [コンポーネント名2]

**役割**: [このコンポーネントが担う責務]

**主要ファイル**:
- `[ファイルパス]` — [役割]

**依存関係**:
- [依存するコンポーネント]

---

## データモデル

### [エンティティ名1]

```
[スキーマ定義 / クラス定義 / テーブル定義]

例（Python dataclass）:
@dataclass
class Task:
    id: str
    title: str
    status: TaskStatus
    created_at: datetime
```

### [エンティティ名2]

```
[スキーマ定義]
```

## 外部依存・連携

| 外部サービス | 用途 | 認証方式 |
|------------|------|---------|
| [例: GitHub API] | [例: Issue 管理] | [例: Personal Access Token] |

## セキュリティ考慮事項

<!-- 例:
- 認証トークンは環境変数で管理し、コードにハードコードしない
- ユーザー入力はすべてバリデーションを通してからDBに渡す
-->
[セキュリティ上の考慮事項を記入]

## パフォーマンス考慮事項

<!-- 例:
- ローカル DB へのクエリは 100ms 以内を目標とする
- 大量タスク（1000件以上）に対してはページネーションを実装する
-->
[パフォーマンス上の考慮事項を記入]
